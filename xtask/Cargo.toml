[package]
name = "xtask"
description = "Repository automation. `cargo xtask verify` is the single definition of done."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0.151"
serde_yaml_bw = "2"

[lints]
workspace = true
