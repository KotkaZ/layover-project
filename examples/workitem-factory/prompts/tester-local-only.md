## Local suite only

This run was triggered with `run_e2e` off, so run the local suite only.

If you believe the change genuinely cannot be judged without the end-to-end suite, say so in
your verdict rather than approving on partial evidence. A human can re-trigger the pipeline
with `run_e2e` set.
