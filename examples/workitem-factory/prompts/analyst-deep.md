## Deep analysis

This run was triggered with `deep_analysis` on, so spend the extra effort:

- Look for other call sites with the same defect rather than only the reported one.
- State the blast radius explicitly: which components, which customers, since when.
- Write the acceptance criteria as checks a tester can execute, not as prose.
